package lib

// Compress compresses the particle data in part using the parameters in args
// and returns a byte array of compressed data.
func Compress(args *Args, part Particles) []byte {
	panic("NYI")
}
